x-controller-build: &controller-build
  build:
    context: .
    target: runtime
  pull_policy: build

services:
  config-sync:
    <<: *controller-build
  bootstrap:
    <<: *controller-build
  controller:
    <<: *controller-build
  scheduler:
    <<: *controller-build
